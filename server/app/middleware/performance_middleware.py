"""Performance monitoring middleware (stub for compatibility)."""

import logging

logger = logging.getLogger(__name__)


def init_performance_middleware(app):
    """Initialize performance middleware (stub)."""
    logger.info("Performance middleware initialized (stub)")
    return True